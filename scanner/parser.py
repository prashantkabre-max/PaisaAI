def parse_market_data(message):
    """
    Parse Upstox live market message.

    Currently this is a placeholder.
    Next we'll extract:
      - Symbol
      - LTP
      - OHLC
      - Volume
      - Timestamp
    """

    if not message:
        return None

    return message
